import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  renderItemContainer: {
    flexDirection: 'row',
    flex: 1,
    borderWidth: 1,
    borderColor: '#DCDCDC',
    overflow: 'scroll',
  },
});
