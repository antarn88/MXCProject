import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  loadingText: {
    fontSize: 16,
    marginLeft: 132,
    marginBottom: 15,
  },
  spinner: {
    marginLeft: 190,
    padding: 15,
    width: 30,
  },
});
